'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _haversine = require('haversine');

var _haversine2 = _interopRequireDefault(_haversine);

var _locations = require('../../locations.json');

var _locations2 = _interopRequireDefault(_locations);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = function (latitude, longitude) {
  var minDistance = Infinity;
  var minLocation = {};

  var start = {
    latitude: latitude,
    longitude: longitude
  };

  for (var i = 0; i < _locations2.default.length; i++) {
    var distance = (0, _haversine2.default)(start, _locations2.default[i]);

    if (distance < minDistance) {
      minLocation = _locations2.default[i];
      minDistance = distance;
    }
  }

  minLocation.distance = minDistance;
  return minLocation;
};